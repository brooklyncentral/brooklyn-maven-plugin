# A blueprint for a single Docker host running in Softlayer Amsterdam.

name: "Docker Cloud environment for tests"

location: jclouds:softlayer:ams01

services:
- type: docker-cloud
  brooklyn.config:
    entity.dynamicLocation.name: "my-docker-cloud"
    docker.version: 1.7.1
